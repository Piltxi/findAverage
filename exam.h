#include <fstream>
#include "list.h"
int countCFU (listNode, const bool); 
float findAverage (listNode); 
void viewExam (listNode, ostream&, bool); 
void addExam (listNode& _exam); 
listNode loadAll (); 